spring:
  datasource:
    url: jdbc:mysql://mysql-ce74a4c-fcktt1-8a03.b.aivencloud.com:21543/defaultdb?useSSL=true&requireSSL=true&verifyServerCertificate=false&allowPublicKeyRetrieval=true
    username: avnadmin
    password: AVNS_aJKvFG1FQmYSn0CbTaE
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        use_sql_comments: true

  # Cấu hình session
  session:
    store-type: none
    timeout: 30m

server:
  port: 8080